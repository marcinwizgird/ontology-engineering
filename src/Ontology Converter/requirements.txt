rdflib>=7.0
networkx>=3.0
falkordb>=1.0    ; only needed for live loading (dry-run works without it)

# for the visualization notebooks under notebooks/
matplotlib>=3.7
pandas>=2.0
jupyterlab>=4.0
nbformat>=5.0
