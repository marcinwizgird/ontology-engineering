Context & Role:
You are an Expert Knowledge Graph Engineer and Python Architect. Your task is to write a production-grade, highly modular ETL pipeline that converts general RDF foundational ontologies into a Meta-Graph in FalkorDB.Core Objective:
Build a Python pipeline that recursively loads an ontology (and its owl:imports), transforms the schema into a networkx.MultiDiGraph as an Intermediate Representation (IR), and ingests it into FalkorDB.Strict Constraints:Schema Only: Do not ingest business data instances. Ignore complex OWL restrictions (Blank Nodes) for this initial MVP.Architecture: Use Python 3.10+. Implement the Strategy Pattern with clear class boundaries separating Extraction (RDF loading), Transformation (NetworkX parsing), and Loading (FalkorDB execution).Idempotency: All database writes must use Cypher MERGE statements, parameterized via the falkordb Python client.1. The Extraction Interface: Recursive Ontology LoaderCreate a class RecursiveOntologyLoader that manages a global rdflib.Graph and a networkx.MultiDiGraph (for ontology module metadata).Behavior: When given a root URI or local file path, it must parse the RDF.Import Traversal: It must query the graph for owl:imports. For every imported URI, it must recursively fetch and parse that ontology into the shared rdflib.Graph.Module Tracking: For every file processed, it must register an (:Ontology) node in the NetworkX graph, capturing its URI. It must draw [:IMPORTS] edges between these ontology nodes based on the owl:imports hierarchy.2. The Transformation Interface: Universal Meta-Graph ParserCreate a class UniversalOntologyConverter that takes the unified rdflib.Graph and populates the networkx.MultiDiGraph. Use rdflib's NamespaceManager to convert URIs to readable QNames (e.g., fibo-fnd:Organization).A. Core Node Mapping Rules:
Ignore all rdflib.BNode (Blank Node) instances entirely.rdf:type owl:Class $\rightarrow$ Add node with label (:OWLClass)rdf:type owl:ObjectProperty $\rightarrow$ Add node with label (:ObjectProperty)rdf:type owl:DatatypeProperty $\rightarrow$ Add node with label (:DatatypeProperty)rdf:type owl:AnnotationProperty $\rightarrow$ Add node with label (:AnnotationProperty)B. Node Attributes:Extract any Literal values (like skos:definition or rdfs:label) attached to a node and store them as simple string key-value pairs inside the NetworkX node's properties dictionary. Ensure property keys are Cypher-safe (e.g., replace : with _).C. Edge Mapping Rules (Schema Topology):
Create edges in NetworkX for the following relationships:rdfs:subClassOf $\rightarrow$ Edge: [:SUBCLASS_OF]rdfs:subPropertyOf $\rightarrow$ Edge: [:SUBPROPERTY_OF]rdfs:domain $\rightarrow$ Edge: [:HAS_DOMAIN] (From Property Node to Class Node)rdfs:range $\rightarrow$ Edge: [:HAS_RANGE] (From Property Node to Class Node)D. Lineage Mapping (Crucial):Look for rdfs:isDefinedBy.For every node, if it has an rdfs:isDefinedBy relationship pointing to an ontology URI, draw an edge: (SchemaNode)-[:DEFINED_IN]->(:Ontology). This connects the schema to the Root Nodes created by the loader.3. The Loading Interface: FalkorDB ExporterCreate a class FalkorDBExporter that accepts the populated networkx.MultiDiGraph.Node Ingestion: Iterate through NetworkX nodes. Execute Cypher: MERGE (n:<Labels> {uri: $uri}) SET n += $props.Edge Ingestion: Iterate through NetworkX edges. Execute Cypher: MATCH (src {uri: $src_uri}), (tgt {uri: $tgt_uri}) MERGE (src)-[r:<EdgeType>]->(tgt).Batching: (Optional but preferred) Group these executions using Cypher UNWIND statements for optimized batch ingestion.4. Output DeliverablesA complete, clean, self-contained Python script implementing the above.Standard library logging included to output progress (e.g., "Recursively fetched: {URI}", "Parsed X nodes").A clear execution block (if __name__ == "__main__":) demonstrating how to wire the Loader, Parser, and Exporter together, pointing to a hypothetical local root_ontology.rdf file.